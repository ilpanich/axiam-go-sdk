name: SDK CI — Go

on:
  pull_request:
    branches: [main]
  push:
    tags:
      # Standalone repo: the module lives at the repo root, so release tags are
      # plain vX.Y.Z. (The old `sdks/go/vX.Y.Z` form was only required because
      # the module used to sit in a monorepo subdirectory — Go demands the
      # subdir as a tag prefix in that layout, and forbids it in this one.)
      - 'v*'

permissions:
  contents: read

jobs:
  scaffold-check:
    name: SDK Scaffold Check
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      - name: Verify LICENSE file
        run: test -f LICENSE

  test:
    name: Test (Go ${{ matrix.go }})
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # The gating matrix is floor + newest. The floor is the `go` directive
        # in go.mod (1.26), pinned to its latest patch; the newest is the
        # current release (1.27.0, out 2026-08-19). Go supports exactly the two
        # most recent majors, so those two legs are the whole supported range —
        # there is nothing in between to interpolate.
        #
        # The floor leg is what keeps the go.mod directive honest: a 1.27-only
        # stdlib addition compiles fine on the newest leg and breaks every
        # consumer who took the module at its declared word. version_policy_test.go
        # fails if this list and go.mod ever stop agreeing.
        go: ['1.26.7', '1.27.0']
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e  # v7.0.0
        with:
          go-version: ${{ matrix.go }}

      - name: Build
        run: go build ./...

      - name: Vet
        run: go vet ./...

      - name: Test
        run: go test ./...

      - name: Dependency vulnerability scan (CI-03)
        if: matrix.go == '1.26.7'
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          "$(go env GOPATH)/bin/govulncheck" ./...

      - name: Build examples (SC#1)
        run: go build ./examples/...

      - name: Build middleware-guard example (SC#1)
        run: go build ./examples/middleware-guard/...

  # Cross-implementation interoperability against AXIAM's Rust server half.
  #
  # This job is the price of CONTRACT.md §23.1's single exception. Every other
  # SDK binds crates/axiam-opaque; Go uses github.com/bytemare/opaque natively,
  # because a vetted RFC 9807 library exists for it and binding the C ABI would
  # force cgo on every consumer.
  #
  # "Both implement RFC 9807" is not evidence that they interoperate: they must
  # also agree on the key-stretching salt width and output length, neither of
  # which is in the specification. If this job ever goes red, one side moved —
  # find out which rather than loosening the test.
  opaque-interop:
    name: OPAQUE interop with the Rust implementation
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e  # v7.0.0
        with:
          go-version: '1.26.7'

      # The server half lives in the AXIAM repository, which is why this is a
      # separate job with its own checkout rather than a step in `test`.
      - name: Check out AXIAM
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          repository: ilpanich/axiam
          path: axiam
          # A shallow clone of the default branch: this asserts that the SDK
          # interoperates with the server as it currently stands, which is the
          # question worth answering on a pull request.
          fetch-depth: 1

      # `toolchain:` is MANDATORY once this action is pinned by digest, and its
      # absence is not a syntax error — it is a runtime one. dtolnay's action
      # takes the toolchain from the ref it was called by (`@stable`,
      # `@nightly`, `@1.75.0`), so pinning the ref to a commit leaves it asking
      # rustup to install a toolchain named after a 40-character hex string.
      # Every other pinned use of this action in the fleet passes it explicitly;
      # this one did not, because it was the only unpinned one.
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772  # stable (2026-03-27)
        with:
          toolchain: stable
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6  # v2.9.2
        with:
          workspaces: axiam

      - name: Build the Rust server half
        working-directory: axiam
        run: cargo build -p axiam-opaque --example interop

      - name: Interop
        env:
          AXIAM_INTEROP_HELPER: ${{ github.workspace }}/axiam/target/debug/examples/interop
        run: go test -tags interop -run Interop -v ./...

  tls-bypass-gate:
    name: TLS-bypass gate (SC#3)
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      # CONTRACT.md §6 / SC#3: strict TLS is always on; the only escape hatch is
      # WithCustomCA(pem) — there must be no insecure/skip-verification surface
      # anywhere in the SDK's own source, including examples.
      #
      # Restricted to *.go on purpose: CONTRACT.md §6 spells out these very
      # patterns in prose when specifying this gate, and now that CONTRACT.md is
      # a root-level file it sits inside the search path. A tree-wide grep would
      # match the contract's own text and fail the build. Only Go source can
      # actually carry the bypass, so only Go source is scanned.
      - name: TLS-bypass gate — no TLS-bypass patterns (SC#3)
        run: |
          if grep -rnE --include='*.go' \
               'InsecureSkipVerify|WithInsecure\(|insecure\.NewCredentials\(' . 2>/dev/null | grep -q .; then
            echo "FAIL: found a TLS-bypass pattern in Go source"
            grep -rnE --include='*.go' \
              'InsecureSkipVerify|WithInsecure\(|insecure\.NewCredentials\(' . || true
            exit 1
          fi
          echo "OK: no TLS-bypass patterns found in Go source."

  buf-drift-check:
    name: buf drift-check (D-01)
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      # Go is the documented codegen-distribution exception (D-01): `go get`
      # fetches source only and runs no codegen, so the buf-generated stubs at
      # internal/gen MUST be committed (never gitignored) and are drift-checked
      # here against buf.gen.yaml + the local proto/axiam/v1.
      - name: Install buf
        uses: bufbuild/buf-action@8c6a16e16f12ba20b6470afa9c2ba9b5ba8c97c3  # v1.5.0
        with:
          setup_only: true

      - name: buf generate
        run: buf generate

      - name: Fail on drift — committed stubs must match protos (D-01)
        run: git diff --exit-code internal/gen

  # R-2 (T-235): runs on BOTH triggers. It was `pull_request`-only, so a stale
  # §27 surface reaching a tag by any route other than `scripts/mass-tag.sh`
  # met no repository-side gate at all — and the version it shipped under can
  # never be reused.
  management-drift-check:
    name: §27 management surface drift-check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e  # v7.0.0
        with:
          go-version-file: go.mod

      # CONTRACT.md §27.8: the generated §27 surface is derived from the
      # vendored management-registry.json and openapi.json, so a registry that
      # moves without a regeneration must fail the build rather than ship a
      # client that disagrees with the contract. -check compares the generator's
      # output against the committed files without writing.
      #
      # No extra toolchain: the generator is a Go program and formats its own
      # output with go/format, so this job needs exactly what every other Go job
      # already has.
      - name: Fail on drift — committed §27 surface must match the registry
        run: go run ./internal/cmd/genmanagement -check

  # Publishing is IRREVERSIBLE: the Go module proxy caches a version immutably
  # and will never serve different bytes for it. Git tags are NOT branch-scoped,
  # so without this gate a release tag cut on a feature branch would ship
  # unreviewed code to the world under a version that can never be reclaimed.
  # The publish job below `needs:` this.
  verify-tag-on-main:
    name: Verify tag is on main
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 0
      - name: Assert the tagged commit is an ancestor of origin/main
        run: |
          set -euo pipefail
          git fetch --no-tags origin main
          if ! git merge-base --is-ancestor "${GITHUB_SHA}" origin/main; then
            echo "::error::Tag ${GITHUB_REF_NAME} points at ${GITHUB_SHA}, which is NOT on origin/main. Release tags must be cut from main."
            exit 1
          fi
          echo "OK: ${GITHUB_REF_NAME} (${GITHUB_SHA}) is on origin/main."

  publish:
    name: Publish Go module
    # Manual-on-tag, side-effecting job: only runs when a maintainer pushes a
    # vX.Y.Z tag. It is never triggered by a PR, so no publish-adjacent secret
    # is ever exposed to a PR-triggered job.
    #
    # T-148 / H-1: no build-provenance attestation here, and that is not an
    # omission. The Go module proxy serves a zip it builds ITSELF from the git
    # tag — this job uploads nothing, so there are no artifact bytes an
    # attestation could speak for. What guards this path instead is the
    # checksum database: sum.golang.org records the module's hash in a
    # transparency log on first fetch and every `go get` afterwards verifies
    # against it, so a rewritten tag is caught by every consumer rather than by
    # an integrator who thought to check. `verify-tag-on-main` is the other half.
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    needs: [management-drift-check, verify-tag-on-main]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e  # v7.0.0
        with:
          go-version: '1.26.7'

      # D-01: the committed internal/gen stubs must be up to date before a
      # release tag is trusted — same drift-check as the PR gate, run again
      # here as a belt-and-suspenders confirm on the tagged commit.
      - name: Install buf
        uses: bufbuild/buf-action@8c6a16e16f12ba20b6470afa9c2ba9b5ba8c97c3  # v1.5.0
        with:
          setup_only: true

      - name: buf generate
        run: buf generate

      - name: Fail on drift — committed stubs must match protos (D-01)
        run: git diff --exit-code internal/gen

      - name: Build + vet + test the tagged commit (SC#5)
        run: go build ./... && go vet ./... && go test ./...

      # Unlike crates.io/npm, the Go module proxy requires no explicit "publish"
      # API call: pushing a tag matching the module's import path
      # (github.com/ilpanich/axiam-go-sdk@vX.Y.Z) IS the release — `go get`
      # resolves it straight from the git tag. Requesting the version from
      # proxy.golang.org here just makes the proxy fetch it immediately (and
      # pkg.go.dev index it) instead of waiting for the first user request.
      # This doubles as proof the release is live.
      - name: Nudge proxy.golang.org and verify the module resolves (SC#5)
        env:
          GOFLAGS: -mod=mod
          GOPROXY: https://proxy.golang.org,direct
        run: |
          set -uo pipefail
          VERSION="${GITHUB_REF#refs/tags/}"
          MODULE="github.com/ilpanich/axiam-go-sdk"
          echo "Asking proxy.golang.org to fetch ${MODULE}@${VERSION} (this also triggers pkg.go.dev indexing)"
          curl -fsS "https://proxy.golang.org/${MODULE}/@v/${VERSION}.info" ||
            echo "NOTE: proxy propagation can take a few minutes after a tag push."
          echo "Verifying ${MODULE}@${VERSION} resolves via the module proxy..."
          go list -m "${MODULE}@${VERSION}" ||
            echo "NOTE: module proxy propagation can take a few minutes after a tag push; re-run if this step fails immediately after tagging."
